// Notación

// ;
const b = 4;
const c = 4

// . ---> Se utiliza en los objetos para acceder a los atributos
// movil.anchura

//[] ---> Listas, arreglos o arrays
const ar = [1, 2, 3, 4];
console.log(ar[2]);

//() ---> Funciones
function suma(a, b) {
    //Se escribe la funcion
}

//{} ---> Para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10,
}

if (true) {
    //Lo que va si es true
} else {
    //Lo que va si es false
}