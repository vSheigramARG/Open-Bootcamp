const alturaEnCm = 178;
const alturaEnMts = 1.78;
const peso = 82.5;
const alturaEnMtsRedondeada = (Math.ceil(alturaEnMts));
const pesoEnMtsRedondeadaHaciaAbajo = (Math.floor(peso));

const sonIguales = Number.MAX_VALUE + 1 === Number.MAX_VALUE;
