nombre = "Alejandro";
apellido = "Espasandín";
estudiante = (nombre + " " + apellido);
estudianteMayus = estudiante.toUpperCase();
estudianteMinus = estudiante.toLowerCase();

const longitudEstudiante = estudiante.length;

const primeraLetra = nombre.charAt(nombre.length[0]);
const ultimaLetra = apellido.charAt(apellido.length -1);

const estudianteSinEspacios = estudiante.split(" ").join("");